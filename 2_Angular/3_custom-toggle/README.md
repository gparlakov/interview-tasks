# Create a custom toggle element
- Discuss what could be possible `@Input()`s for such component
- Create a component from scratch (Angular basics, html structuring, css knowledge)
- No animations required